# Install Directories

Generated from built-in Botstack manifests.

## GBrain

- `~/.botstack/tools/gbrain` — Create the GBrain tool directory
- `~/.codex/config.toml` (codex) — Register GBrain notes for Codex users
- `~/.config/opencode/opencode.json` (opencode) — Register GBrain notes for OpenCode users
- `~/.claude.json` (claude) — Register GBrain notes for Claude Code users
- `~/.continue/config.json` (continue) — Register GBrain notes for Continue users

## Graphify

- `~/.botstack/tools/graphify` — Create the Graphify tool directory

## GStack

- `~/.botstack/tools/gstack` — Create the GStack tool directory
- `~/.codex/AGENTS.md` (codex) — Add a managed note about GStack routing
- `~/.config/opencode/opencode.json` (opencode) — Record GStack routing notes for OpenCode users
- `~/.claude.json` (claude) — Record GStack routing notes for Claude Code users

## Serena MCP

- `~/.botstack/tools/serena` — Create the Serena tool directory
- `~/.codex/config.toml` (codex) — Record Serena MCP enablement for Codex users
- `~/.config/opencode/opencode.json` (opencode) — Record Serena MCP enablement for OpenCode users
- `~/.claude.json` (claude) — Record Serena MCP enablement for Claude Code users

